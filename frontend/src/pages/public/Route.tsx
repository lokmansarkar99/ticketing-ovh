import PageWrapper from "@/components/common/wrapper/PageWrapper";
import { FC } from "react";

interface IRouteProps {}

const Route: FC<IRouteProps> = () => {
  return <PageWrapper> Route</PageWrapper>;
};

export default Route;
